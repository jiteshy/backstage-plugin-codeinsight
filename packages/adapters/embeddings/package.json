{
  "name": "@codeinsight/embeddings",
  "version": "0.1.0",
  "description": "Embedding client implementations (OpenAI) with caching for CodeInsight",
  "license": "Apache-2.0",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": ["dist"],
  "scripts": {
    "build": "tsc --build",
    "clean": "rm -rf dist *.tsbuildinfo"
  },
  "dependencies": {
    "@codeinsight/types": "workspace:*",
    "knex": "^3.1.0",
    "openai": "^4.85.0"
  },
  "devDependencies": {
    "typescript": "~5.4.0"
  }
}
