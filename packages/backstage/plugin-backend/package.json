{
  "name": "@codeinsight/plugin-backend",
  "version": "0.1.0",
  "description": "CodeInsight Backstage backend plugin — wires adapters into core services",
  "license": "Apache-2.0",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "backstage": {
    "role": "backend-plugin"
  },
  "scripts": {
    "build": "tsc --build",
    "clean": "rm -rf dist *.tsbuildinfo"
  },
  "configSchema": "config.d.ts",
  "files": ["dist", "config.d.ts"],
  "dependencies": {
    "@backstage/backend-plugin-api": "^1.7.0",
    "@codeinsight/ingestion": "workspace:*",
    "@codeinsight/repo": "workspace:*",
    "@codeinsight/storage": "workspace:*",
    "@codeinsight/types": "workspace:*",
    "express": "^4.18.2",
    "express-promise-router": "^4.1.0"
  },
  "devDependencies": {
    "@backstage/backend-test-utils": "^1.1.0",
    "@types/express": "^4.17.21",
    "typescript": "~5.4.0"
  }
}
